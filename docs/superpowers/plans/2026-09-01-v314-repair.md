# ReConfig 3.1.4 Repair Plan

1. Add source-level and runtime regression tests for ReConfig-only search, emoji asset tokens, focused Right Shift, HUD visibility, vanilla-pass crosshair replacement, independent freelook rotation, toggle sprint state, and version metadata.
2. Refactor social UI controls around shared eased interaction states and asset-backed emoji messages.
3. Route global search through the ReConfig corpus and reuse bundled settings controls only inside ReConfig pages.
4. Gate HUD rendering by gameplay/HUD-editor state and move visual panels to the shared anti-aliased surface.
5. Render the custom crosshair inside `Gui.renderCrosshair`, separate freelook camera rotation from player rotation, and replace the sprint supplier redirect with explicit state handling.
6. Run focused tests, backend tests, Gradle tests, and `buildAndCollect`; package the resulting source and artifacts.
